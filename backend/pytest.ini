[pytest]
# Lets `from services.ocr_service import ...` etc. resolve when running
# `pytest` from the backend/ directory, without needing an installed
# package or a conftest.py sys.path hack.
pythonpath = .
testpaths = tests
